const initialState = {
  company: "ekeekaran ventures pvt ltd",
};

function orders(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default orders;
